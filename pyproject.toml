[tool.ruff]
line-length = 100
target-version = "py311"
exclude = [
    "frontend",
    ".venv",
    "venv",
    "alembic/versions",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["B008", "B019"]
